module.exports = {
    dist: {
        files: {
            // Override from the task
        }
    },
    templates: {
        files: {
            // Override from the task
        }
    },
    modules: {
        files: {
            // Override from the task
        }
    }
};